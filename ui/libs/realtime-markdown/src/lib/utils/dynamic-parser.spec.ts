describe('dynamic parser', function () {
  test('should parse only on special chars, enter, ...', () => {
    //
  });
  test('should skip on a-zA-Z0-9 chars', () => {
    //
  });
  test('should parse on content modification', () => {
    //
  });
  test('should parse on sign modification', () => {
    //
  });
  test('should parse on shortcut', () => {
    //
  });
});
